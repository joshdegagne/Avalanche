////////////////////////////////////////////////////////////////////////////////
// Filename: PlayerBumpState.h
////////////////////////////////////////////////////////////////////////////////
#pragma once
#include "PlayerState.h"

////////////////////////////////////////////////////////////////////////////////
// Class Name: PlayerBumpState
////////////////////////////////////////////////////////////////////////////////
class PlayerBumpState : public PlayerState {
public:
	PlayerBumpState(Player&, bool, float = PS_BUMP_DURATION);
	~PlayerBumpState();

	void initialize();
	void update(float);
	void timerCallback(Timer&);
	bool isBumpedLeft();

private:
	void  updateBumpArc();
	float getBumpArc(float);
	bool bumpedLeft;
};